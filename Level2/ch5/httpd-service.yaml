apiVersion: v1
kind: Service
metadata:
  name: httpd-service
  namespace: nautilus
spec:
 type: NodePort
 selector:
   app: httpd-deploy
 ports:
   - protocol: TCP
     port: 80
     targetPort: 80
     nodePort: 30008